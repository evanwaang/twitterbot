interface Config{
    searchQuery: string,
    message: string
}

let config: Config = {
    searchQuery: "#scalping",
    message: "Hey! my name is Evan, hows trading been for you?"
}

export default config